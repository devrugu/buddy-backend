[build]
  # Specify your build commands here
  commands = [
    "composer install"
  ]

[deploy]
  # Specify your deployment commands here
  commands = [
    "service nginx restart",  # Restart Nginx to apply the new configuration
    "php-fpm"  # Start PHP-FPM if needed
  ]
